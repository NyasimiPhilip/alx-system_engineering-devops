#!/usr/bin/env bash
# This script displays the message "Best School" 10 times using an until loop.

# Initialize a variable to control the loop
i=0

# Use an until loop to repeat the message 10 times
until ((i >= 10))
do
    echo "Best School"  # Output the message
    ((i++))  # Increment the loop control variable
done
